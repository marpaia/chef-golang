#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export DH_OPTIONS

export DH_GOPKG := github.com/marpaia/chef-golang

override_dh_auto_test:

%:
	dh $@ --buildsystem=golang --with=golang
